apiVersion: v1
kind: Service
metadata:
  name: {{ include "meter.fullname" . }}
  labels:
    {{- include "meter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "meter.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
